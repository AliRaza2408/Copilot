# Frontend Source

## What this folder contains
- React entrypoints, shared styles, assets, pages, components, and API service modules.

## How it fits into the project
- Houses all runtime UI code for the Copilot frontend experience.

## Important notes
- Keep reusable UI in `components/` and route-level composition in `pages/`.
