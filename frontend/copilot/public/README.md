# Public Assets

## What this folder contains
- Static files served directly by Vite at build/runtime without import processing.

## How it fits into the project
- Hosts web assets that must keep stable public paths.

## Important notes
- Prefer `src/assets/` for imported UI assets; use this folder only for direct public-path files.
