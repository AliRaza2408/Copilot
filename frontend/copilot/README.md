# Copilot Frontend App

## What this folder contains
- The Vite + React application, package configuration, and frontend build tooling.

## How it fits into the project
- Renders the user workflow for document upload, analysis, and recommendation review.

## Important notes
- Core app code is under `src/`.
- API integration helpers are in `src/services/api.js`.
