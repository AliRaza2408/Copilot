# Copilot Repository

## What this folder contains
- Backend services, frontend app, sample data, and evaluation assets for the Copilot project.

## How it fits into the project
- This is the project root used to run and develop both application layers together.

## Important notes
- Use `/backend` for API and decision logic changes.
- Use `/frontend/copilot` for UI changes.
