# Backend

## What this folder contains
- FastAPI entrypoints, domain modules, service layer code, tests, and backend dependencies.

## How it fits into the project
- Implements document analysis, extraction, decisioning, and reliability checks used by the UI and API clients.

## Important notes
- Runtime upload artifacts are stored in `uploads/`; avoid treating that folder as source code.
